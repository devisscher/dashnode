extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    if directories
      for directory in directories
        li #{directory}
  textarea(style="width:100%;height:70vh;") #{data}